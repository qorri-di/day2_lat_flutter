import 'package:flutter/material.dart';

class FoodBuilder extends StatelessWidget {
  const FoodBuilder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
